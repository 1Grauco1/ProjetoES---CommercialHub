'use client';

import DashboardShell from '@/src/components/dashboard/DashboardShell';
import PageHeader from '@/src/components/dashboard/PageHeader';
import { useSalas } from '@/src/hooks/use-salas';
const money = new Intl.NumberFormat('pt-BR', { style: 'currency', currency: 'BRL' });
export default function FaturamentoPage() { const { salas, loading, error } = useSalas(); const monthly = salas.filter(sala => sala.status_ocupacao === 'Alugada').reduce((total, sala) => total + sala.preco, 0); const annual = monthly * 12; return <DashboardShell><PageHeader title="Faturamento" description="Visão geral do faturamento e receitas." />{error && <p className="mb-6 rounded-xl border border-red-200 bg-red-50 p-4 text-sm text-red-700">{error}</p>}<section className="grid gap-5 md:grid-cols-2"><article className="rounded-2xl bg-white p-7 shadow-sm ring-1 ring-slate-200"><p className="text-sm font-medium text-slate-500">Receita anual projetada</p><p className="mt-3 text-3xl font-bold">{loading ? '—' : money.format(annual)}</p><p className="mt-2 text-sm text-emerald-600">Baseada nos aluguéis ativos</p></article><article className="rounded-2xl bg-white p-7 shadow-sm ring-1 ring-slate-200"><p className="text-sm font-medium text-slate-500">Receita mensal</p><p className="mt-3 text-3xl font-bold">{loading ? '—' : money.format(monthly)}</p><p className="mt-2 text-sm text-slate-500">{salas.filter(sala => sala.status_ocupacao === 'Alugada').length} salas alugadas</p></article></section><article className="mt-7 rounded-2xl bg-white p-7 shadow-sm ring-1 ring-slate-200"><h2 className="font-bold">Receita por imóvel alugado</h2>{!loading && salas.filter(sala => sala.status_ocupacao === 'Alugada').length === 0 ? <p className="mt-6 text-sm text-slate-500">Ainda não há imóveis alugados para gerar faturamento.</p> : <div className="mt-9 flex h-64 items-end gap-3">{salas.filter(sala => sala.status_ocupacao === 'Alugada').map(sala => <div className="flex flex-1 flex-col items-center gap-3" key={sala.id}><div className="w-full rounded-t-md bg-[#D35400]" style={{ height: `${monthly ? Math.max((sala.preco / monthly) * 100, 8) : 0}%` }} /><span className="text-xs text-slate-500">Sala #{sala.id}</span><span className="text-xs font-semibold">{money.format(sala.preco)}</span></div>)}</div>}</article></DashboardShell>; }
